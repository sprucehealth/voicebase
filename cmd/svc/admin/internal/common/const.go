package common

// TODO: Better name for this package? But want a place to constify and unite common values.

const (
	// AuthCookieName represent the name assigned to the authentication token cookie
	AuthCookieName = "admin_at"

	// InputFieldName defines the field name that should be applied to all mutations requiring inputs
	InputFieldName = "input"

	// UIDContextFieldName defines the name of the context field for staring uid
	UIDContextFieldName = "uid"
)
